<Window x:Class="StartitsinWPF.PartnerForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Партнёр — Staritsin"
        Height="580" Width="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <DockPanel LastChildFill="True">

        <Border DockPanel.Dock="Top" Style="{StaticResource WindowHeaderStyle}">
            <TextBlock x:Name="FormTitleText"
                       Text="Добавление партнёра"
                       FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>

        <Border DockPanel.Dock="Bottom"
                Background="White"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="20,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена"
                        Width="100"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,8,0"
                        Click="Cancel_Click"/>
                <Button Content="💾  Сохранить"
                        Width="130"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="Save_Click"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,16">

                <TextBlock Text="Наименование компании *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox x:Name="CompanyNameBox" Margin="0,0,0,12"/>

                <TextBlock Text="Тип партнёра *" Style="{StaticResource FormLabelStyle}"/>
                <ComboBox x:Name="PartnerTypeCombo"
                          DisplayMemberPath="TypeName"
                          SelectedValuePath="Id"
                          Margin="0,0,0,12"/>

                <TextBlock Text="ФИО директора *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox x:Name="DirectorNameBox" Margin="0,0,0,12"/>

                <TextBlock Text="Телефон *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox x:Name="PhoneBox"
                         ToolTip="Пример: +7 495 123 45 67"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Email *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox x:Name="EmailBox"
                         ToolTip="Пример: info@company.ru"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Юридический адрес *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox x:Name="AddressBox"
                         Height="64"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Рейтинг (целое число ≥ 0) *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox x:Name="RatingBox"
                         PreviewTextInput="RatingBox_PreviewTextInput"
                         ToolTip="Введите целое неотрицательное число, например: 7"
                         Margin="0,0,0,4"/>
                <TextBlock Text="Допустимые значения: 0 и выше"
                           FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,16"/>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
